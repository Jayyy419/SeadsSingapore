## Summary

<!-- What does this change, and why? -->

## Test plan

<!-- How was this verified? (manual click-through, curl against the live API, etc.) -->

## Checklist

- [ ] `npm run lint` / `npx tsc --noEmit` / `npm run build` pass locally (CI will also check this)
- [ ] If this touches `backend/interest-form/index.mjs`, ran `node --check index.mjs` locally
- [ ] Updated `docs/CHANGELOG.md` if this is a user-facing or infra change
